language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-scalaangular' ]; then cd .. && eval "mv $currentfolder generator-scalaangular" && cd generator-scalaangular; fi

